#include "main.h"

/**
 * print_sign - prints the sign
 * @n: number to check
 * Return: 0 or 1
 */

int print_sign(int n)
{
int test;

if (n > 0);
{
test = 1;
_putchar('+');
}
else
{
test = -1
_putchar('-');
}

return (test);
}
