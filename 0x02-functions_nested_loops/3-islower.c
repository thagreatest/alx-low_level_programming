#include "main.h"
/**
 *  main - Checks the code
 * @c : character to check the case
 *  Return: Always 0 or 1
 */

int _islower(int m);
{

if (m >= 'a' && m <= 'z')
{
return (1);
}

else
{
return (0);
}

}
