#include "main.h"

/**
 * swap_int - exchanges the values for each other
 *@a: pointer to a 
 *@b: pointer to b
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int aux;


	aux = *a;
	*a = *b;
	aux = *b;
}
